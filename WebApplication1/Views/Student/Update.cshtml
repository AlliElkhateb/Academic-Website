@model StudentVM

@{
    ViewData["Title"] = "Update";

    SelectList departmentsList;
    if (Model is not null)
    {
        departmentsList = new(Model.Departments, "Id", "Name", Model.Student.DepartmentId);
    }
    else
    {
        departmentsList = new(Model.Departments, "Id", "Name");
    }
}

<h2>Update</h2>
<hr />
<h4>Student</h4>
<section class="row justify-content-between">
    <div class="col-md-4">
        <form asp-action="update" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="hidden" asp-for="Student.Id" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Student.Name" class="control-label"></label>
                <input asp-for="Student.Name" class="form-control" />
                <span asp-validation-for="Student.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Student.Age" class="control-label"></label>
                <input asp-for="Student.Age" class="form-control" value="@(Model.Student.Age > 0 ? Model.Student.Age : string.Empty)" />
                <span asp-validation-for="Student.Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Student.Address" class="control-label"></label>
                <input asp-for="Student.Address" class="form-control" />
                <span asp-validation-for="Student.Address" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Student.DepartmentId" class="control-label"></label>
                <select asp-for="Student.DepartmentId" asp-items="departmentsList" class="form-select"></select>
                <span asp-validation-for="Student.DepartmentId" class="text-danger"></span>
            </div>
            <div class="form-group mt-2">
                <button type="submit" class="btn btn-success me-2">
                    <i class="bi bi-check-lg"></i> Save
                </button>
                <a asp-action="index" class="btn btn-secondary"><i class="bi bi-x-lg"></i> Cancel</a>
            </div>
        </form>
    </div>
</section>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}



    @*            <div>choose courses</div>
    <div class="btn-group mt-1 mb-1" role="group" aria-label="Basic checkbox toggle button group">
    @foreach (var course in Model.Courses)
    {
    @if (Model.ListOfCoursesId.Contains(course.Id))
    {
    <input type="checkbox" class="btn-check" name="ListOfCoursesId" value="@course.Id" id="@course.Id" autocomplete="off" checked />
    <label class="btn btn-outline-primary" for="@course.Id">@course.Name</label>
    }
    else
    {
    <input type="checkbox" class="btn-check" name="ListOfCoursesId" value="@course.Id" id="@course.Id" autocomplete="off" />
    <label class="btn btn-outline-primary" for="@course.Id">@course.Name</label>
    }
    }
    </div>
    <br />
    <span asp-validation-for="ListOfCoursesId" class="text-danger"></span>*@